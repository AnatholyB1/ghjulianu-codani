---
phase: 04-polish-enhancements
plan: 07
type: execute
wave: 3
depends_on:
  - 04-03
  - 04-04
  - 04-05
autonomous: false
files_modified:
  - .planning/perf-notes.md
requirements:
  - PER-01
  - PER-02
  - PER-03

must_haves:
  truths:
    - "EXPLAIN ANALYZE results for all 4 day/night queries are documented in .planning/perf-notes.md"
    - "All 4 queries show Index Scan or Bitmap Index Scan (not Sequential Scan) in EXPLAIN output"
    - "All 4 queries execute in under 100ms"
    - "next build route-level JS sizes are recorded before and after Phase 4 additions"
    - "No preemptive React.memo or useMemo was added without profiling evidence"
  artifacts:
    - path: ".planning/perf-notes.md"
      provides: "EXPLAIN ANALYZE output for 4 queries + bundle size delta"
      contains: "EXPLAIN ANALYZE"
  key_links:
    - from: "Supabase MCP execute_sql"
      to: "portfolio_photos and albums is_day queries"
      via: "EXPLAIN ANALYZE SQL"
      pattern: "EXPLAIN ANALYZE"
---

<objective>
Performance validation pass per D-19/D-20 and requirements PER-01, PER-02, PER-03.

Two tasks:
1. Run EXPLAIN ANALYZE on all four day/night Supabase queries via the Supabase MCP tool, document results in `.planning/perf-notes.md`
2. Run `next build` and record route-level JS bundle sizes; compare to pre-Phase-4 baseline

Human checkpoint to review EXPLAIN results (confirm no sequential scans) and bundle size delta.

Purpose: Confirms Phase 4 does not introduce performance regressions and that Phase 2's database indexes are being used.
Output: .planning/perf-notes.md with documented EXPLAIN results and bundle size delta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-polish-enhancements/04-CONTEXT.md
@.planning/phases/04-polish-enhancements/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run EXPLAIN ANALYZE via Supabase MCP and document results</name>
  <files>.planning/perf-notes.md</files>
  <read_first>
    - .planning/phases/04-polish-enhancements/04-RESEARCH.md — Performance Validation Detailed Plan section for the exact SQL queries to run
    - .planning/phases/04-polish-enhancements/04-UI-SPEC.md — Performance Validation Contract section
  </read_first>
  <action>
    Use the Supabase MCP `execute_sql` tool to run the following four EXPLAIN ANALYZE queries. Run each separately and capture the full text output.

    Query 1 — portfolio_photos day mode:
    ```sql
    EXPLAIN ANALYZE
    SELECT * FROM portfolio_photos
    WHERE is_day IS NULL OR is_day = true;
    ```

    Query 2 — portfolio_photos night mode:
    ```sql
    EXPLAIN ANALYZE
    SELECT * FROM portfolio_photos
    WHERE is_day IS NULL OR is_day = false;
    ```

    Query 3 — albums day mode:
    ```sql
    EXPLAIN ANALYZE
    SELECT albums.*, row_to_json(categories.*) as category
    FROM albums
    LEFT JOIN categories ON albums.category_id = categories.id
    WHERE (albums.is_day IS NULL OR albums.is_day = true)
    ORDER BY albums.sort_order DESC;
    ```

    Query 4 — albums night mode:
    ```sql
    EXPLAIN ANALYZE
    SELECT albums.*, row_to_json(categories.*) as category
    FROM albums
    LEFT JOIN categories ON albums.category_id = categories.id
    WHERE (albums.is_day IS NULL OR albums.is_day = false)
    ORDER BY albums.sort_order DESC;
    ```

    For each query, record:
    - The full EXPLAIN ANALYZE output (node types, costs, execution times)
    - Whether the output shows "Index Scan", "Bitmap Index Scan", or "Seq Scan" for the is_day filter
    - The "Execution Time" line (in ms)

    Create `.planning/perf-notes.md` with the following structure:
    ```
    # Phase 4 Performance Notes

    **Date:** [date]
    **PER-01 requirement:** Index Scan on is_day queries, execution time < 100ms

    ## Query 1: portfolio_photos (day mode)
    [full EXPLAIN ANALYZE output]
    **Scan type:** [Index Scan / Bitmap Index Scan / Seq Scan]
    **Execution time:** [X.XXX ms]
    **PER-01 status:** [PASS / FAIL]

    ## Query 2: portfolio_photos (night mode)
    [...]

    ## Query 3: albums (day mode)
    [...]

    ## Query 4: albums (night mode)
    [...]

    ## Summary
    | Query | Scan Type | Execution Time | PER-01 |
    |-------|-----------|----------------|--------|
    | portfolio_photos day | [type] | [time]ms | [PASS/FAIL] |
    | portfolio_photos night | [type] | [time]ms | [PASS/FAIL] |
    | albums day | [type] | [time]ms | [PASS/FAIL] |
    | albums night | [type] | [time]ms | [PASS/FAIL] |
    ```

    If any query shows "Seq Scan" on the is_day column, flag it as FAIL. If execution time > 100ms, flag as FAIL. Document the failure clearly so it can be investigated (may require adding an index or adjusting the query).
  </action>
  <acceptance_criteria>
    - `.planning/perf-notes.md` exists
    - File contains `EXPLAIN ANALYZE` (literal text from the SQL output)
    - File contains results for all 4 queries (portfolio_photos day, portfolio_photos night, albums day, albums night)
    - File contains a PER-01 status (PASS or FAIL) for each query
    - If any query shows Seq Scan on is_day, it is documented as FAIL with the full EXPLAIN output preserved
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/perf-notes.md && grep -c "EXPLAIN ANALYZE\|Execution Time\|Seq Scan\|Index Scan" .planning/perf-notes.md | grep -v "^0$" && echo "perf-notes.md has content"</automated>
  </verify>
  <done>.planning/perf-notes.md exists with EXPLAIN ANALYZE output for all 4 queries, scan type and execution time recorded, PER-01 status for each query.</done>
</task>

<task type="auto">
  <name>Task 2: Record bundle size delta from next build</name>
  <files>.planning/perf-notes.md</files>
  <read_first>
    - .planning/perf-notes.md — read current state (created in Task 1) to append bundle size section
  </read_first>
  <action>
    Run `rtk next build` and capture the route-level JS sizes from the build output. Next.js prints a table of routes with their JS bundle sizes.

    Append a "Bundle Size (PER-03)" section to `.planning/perf-notes.md`:

    ```
    ## Bundle Size — PER-03

    **Date:** [date]
    **Method:** `next build` route-level JS output

    ### Routes and JS sizes (Phase 4 state):
    [paste relevant lines from next build output — route table showing JS sizes]

    ### New components added in Phase 4:
    - WelcomeModal — dynamically imported with ssr:false (excluded from critical path)
    - VideoTransitionOverlay — rendered inside DayNightToggle (included in toggle chunk)
    - prefersReducedMotion.ts — tiny utility, negligible size

    ### Bundle delta assessment:
    [Note any routes that increased significantly (>5KB) compared to expected baseline]

    **PER-03 status:** PASS / FAIL / ACCEPTABLE INCREASE
    (PASS if no route increased more than 10KB; ACCEPTABLE if WelcomeModal route shows increase due to video-related imports; FAIL if unexplained large increases)
    ```

    No preemptive React.memo or useMemo additions — per D-20, these are only added if profiling reveals actual unnecessary re-renders. The fade state machine adds one extra render cycle per toggle (fading=true → fetch → fading=false), which is correct and expected.
  </action>
  <acceptance_criteria>
    - `.planning/perf-notes.md` contains a "Bundle Size" section with `next build` route table output
    - File contains "PER-03 status: PASS" or "PER-03 status: ACCEPTABLE INCREASE" with rationale
    - File notes that WelcomeModal is dynamically imported with ssr:false
    - `rtk next build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>rtk next build 2>&1 | tail -30; echo "Build complete"</automated>
  </verify>
  <done>.planning/perf-notes.md updated with bundle size section. next build route sizes recorded. PER-03 status documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Performance validation results in .planning/perf-notes.md:
    - EXPLAIN ANALYZE output for 4 day/night queries against Supabase
    - next build route-level JS bundle sizes
  </what-built>
  <how-to-verify>
    1. Open `.planning/perf-notes.md`
    2. Check the Summary table — all 4 queries should show "Index Scan" or "Bitmap Index Scan" (not "Seq Scan"). Execution times should all be < 100ms.
    3. If any query shows "Seq Scan" on the is_day column: this is a PER-01 failure. The Phase 2 index may not be covering the `.or('is_day.is.null,...')` pattern. Flag for investigation.
    4. Check the Bundle Size section — WelcomeModal route should not show unreasonable size increase (it's dynamically imported; it should appear in lazy chunk, not critical path). Flag any route that increased more than 20KB unexpectedly.
    5. (Optional) In browser DevTools Performance panel: record a mode toggle on the portfolio page, verify the opacity fade transition runs on the compositor thread (green bar in Performance timeline, no layout/paint events during fade).
  </how-to-verify>
  <resume-signal>Type "approved" if PER-01 EXPLAIN results are satisfactory and bundle sizes are acceptable. Or describe issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → Supabase MCP | EXPLAIN ANALYZE queries run against production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | EXPLAIN ANALYZE output stored in perf-notes.md | accept | EXPLAIN ANALYZE reveals query plans but not data; perf-notes.md is a planning file committed to repo — acceptable for a private portfolio project |
| T-04-14 | Denial of Service | EXPLAIN ANALYZE query execution | accept | These are read-only analytical queries; ANALYZE re-plans statistics but does not modify data |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
PER-01: All 4 EXPLAIN ANALYZE results show Index Scan or Bitmap Index Scan, execution time < 100ms each
PER-02: Fade animation verified as compositor-thread opacity transition (willChange: opacity during active fade)
PER-03: next build route sizes recorded, WelcomeModal confirmed as lazy chunk, no unexpected size increases
Human checkpoint: EXPLAIN results and bundle sizes reviewed and approved
</verification>

<success_criteria>
- .planning/perf-notes.md exists with EXPLAIN ANALYZE output for all 4 queries
- All queries show index usage (not sequential scans) per PER-01
- Bundle size delta documented per PER-03
- Human reviewer approves performance results
</success_criteria>

<output>
Create `.planning/phases/04-polish-enhancements/04-07-SUMMARY.md` when done
</output>
