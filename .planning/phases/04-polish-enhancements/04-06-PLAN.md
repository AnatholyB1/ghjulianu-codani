---
phase: 04-polish-enhancements
plan: 06
type: execute
wave: 3
depends_on:
  - 04-03
  - 04-04
  - 04-05
autonomous: false
files_modified: []
requirements:
  - ACC-01
  - ACC-02
  - ACC-03

must_haves:
  truths:
    - "DayNightToggle is operable via keyboard (Space/Enter) with visible focus indicator"
    - "WelcomeModal has working focus trap — Tab cycles only within modal, Escape dismisses"
    - "DayNightToggle aria-label is contextual French ('Activer le mode jour' / 'Activer le mode nuit') and aria-pressed reflects current state"
    - "PortfolioGrid and AlbumsDragTrack aria-live regions announce content loading to screen readers"
    - "All new component text (WelcomeModal headline, buttons) meets WCAG AA contrast"
  artifacts:
    - path: "src/components/WelcomeModal.tsx"
      provides: "Focus trap verified, ARIA structure verified"
    - path: "src/components/DayNightToggle.tsx"
      provides: "Contextual French ARIA labels, aria-pressed, keyboard operability"
  key_links:
    - from: "WelcomeModal focus trap"
      to: "Tab key behavior"
      via: "querySelectorAll('button') keydown handler"
      pattern: "focus"
    - from: "DayNightToggle"
      to: "aria-pressed attribute"
      via: "mode === 'day'"
      pattern: "aria-pressed"
---

<objective>
Accessibility verification and hardening pass — confirm all ACC-01, ACC-02, ACC-03 requirements are met across the Phase 4 implementations.

This plan is primarily a human-checkpoint plan. Automated checks (build passes, grep assertions) confirm code presence; human verification confirms actual browser behavior (keyboard navigation, screen reader announcements, contrast ratios, focus management).

If gaps are found during verification, they are fixed in this plan before declaring Phase 4 complete.

Purpose: Ensures Phase 4 delivers WCAG AA compliance and full keyboard accessibility, not just code that looks correct.
Output: Verified and fixed accessibility behavior across WelcomeModal, DayNightToggle, PortfolioGrid, AlbumsDragTrack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-polish-enhancements/04-CONTEXT.md
@.planning/phases/04-polish-enhancements/04-UI-SPEC.md
@.planning/phases/04-polish-enhancements/04-04-SUMMARY.md
@.planning/phases/04-polish-enhancements/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated accessibility structure assertions</name>
  <files></files>
  <read_first>
    - src/components/WelcomeModal.tsx — read to confirm ARIA attributes, focus trap implementation, copy text
    - src/components/DayNightToggle.tsx — read to confirm aria-label, aria-pressed, icon animation
    - src/app/portfolio/PortfolioGrid.tsx — read to confirm aria-live region is present
    - src/app/albums/AlbumsDragTrack.tsx — read to confirm aria-live region is present
  </read_first>
  <action>
    Run grep assertions to confirm all accessibility structures are present in the implementation. Document findings. If any assertion fails, fix the corresponding file.

    Required assertions (run each):
    1. `grep -c 'aria-pressed' src/components/DayNightToggle.tsx` → must return ≥ 1
    2. `grep -c 'Activer le mode' src/components/DayNightToggle.tsx` → must return ≥ 1
    3. `grep -c 'role="dialog"' src/components/WelcomeModal.tsx` → must return ≥ 1
    4. `grep -c 'aria-modal="true"' src/components/WelcomeModal.tsx` → must return ≥ 1
    5. `grep -c 'aria-labelledby="welcome-title"' src/components/WelcomeModal.tsx` → must return ≥ 1
    6. `grep -c 'aria-describedby="welcome-desc"' src/components/WelcomeModal.tsx` → must return ≥ 1
    7. `grep -c 'querySelectorAll' src/components/WelcomeModal.tsx` → must return ≥ 1 (focus trap)
    8. `grep -c "Escape" src/components/WelcomeModal.tsx` → must return ≥ 1 (Escape dismiss)
    9. `grep -c 'aria-live="polite"' src/app/portfolio/PortfolioGrid.tsx` → must return ≥ 1
    10. `grep -c 'aria-live="polite"' src/app/albums/AlbumsDragTrack.tsx` → must return ≥ 1
    11. `grep -c 'Chargement du contenu' src/app/portfolio/PortfolioGrid.tsx` → must return ≥ 1
    12. `grep -c 'Chargement du contenu' src/app/albums/AlbumsDragTrack.tsx` → must return ≥ 1
    13. `grep -c 'ghjulianu-welcomed' src/components/WelcomeModal.tsx` → must return ≥ 2 (one read, one write)

    For any assertion that fails (returns 0), fix the corresponding component file to add the missing attribute or text. Re-run the assertion after fixing.

    Run `rtk next build` after all fixes to confirm build still passes.
  </action>
  <acceptance_criteria>
    - All 13 grep assertions return ≥ 1 (or ≥ 2 for assertion 13)
    - `rtk next build` exits 0 after any fixes
    - Document which assertions passed and which required fixes in a brief note
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'aria-pressed' src/components/DayNightToggle.tsx && grep -c 'role="dialog"' src/components/WelcomeModal.tsx && grep -c 'aria-live="polite"' src/app/portfolio/PortfolioGrid.tsx && echo "Core ARIA assertions passed"</automated>
  </verify>
  <done>All 13 accessibility structure assertions pass. Any gaps found were fixed. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 accessibility implementations:
    - DayNightToggle: Space/Enter keyboard toggle, contextual French aria-label, aria-pressed
    - WelcomeModal: focus trap (Tab cycles JOUR/NUIT only), Escape to dismiss, role="dialog"
    - PortfolioGrid + AlbumsDragTrack: aria-live regions announce content loading
    - WelcomeModal: [JOUR] button on #E8E4DC bg with #080808 text (~18:1), [NUIT] button #E8E4DC on dark scrim (~16:1)
    - Toggle icons: Sun (#c8a97e on #080808 ~9:1), Moon (#7a5200 variant on #fafafa ~6.6:1)
  </what-built>
  <how-to-verify>
    **ACC-01 — Keyboard Navigation:**
    1. Tab to the DayNightToggle button in the navbar. Confirm it receives visible focus indicator.
    2. Press Space or Enter — confirm mode toggles (icon animates, page theme changes).
    3. Clear localStorage, reload. When WelcomeModal appears, Tab through it. Confirm focus cycles only between [JOUR] and [NUIT] — Tab on [NUIT] returns focus to [JOUR], Shift+Tab on [JOUR] moves to [NUIT].
    4. While WelcomeModal is open, press Escape. Confirm modal dismisses without changing mode.

    **ACC-02 — Screen Reader / ARIA:**
    5. In browser DevTools (Accessibility tab or Inspect Element > Accessibility):
       - DayNightToggle button: check `aria-label` is "Activer le mode jour" or "Activer le mode nuit" (not "Toggle day/night mode"), and `aria-pressed` attribute is present
       - WelcomeModal: check outer div has `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby`
    6. (Optional) Use VoiceOver (Mac: Cmd+F5) or NVDA: toggle mode in PortfolioGrid — confirm a live region announcement is read aloud ("Chargement du contenu..." then "N photos chargées").

    **ACC-03 — WCAG AA Contrast:**
    7. Open WelcomeModal (clear localStorage to trigger). Check the [JOUR] button (light bg, dark text) and [NUIT] button (transparent bg, light text over dark scrim). Either use Chrome DevTools Accessibility panel or browser developer tools to verify contrast. Both should pass WCAG AA (≥ 4.5:1 for normal text).
    8. In day mode: DayNightToggle shows Moon icon (#7a5200 or similar on #fafafa). In night mode: Sun icon (#c8a97e on #080808). Verify both are visually readable.

    If any item fails, describe the issue in detail.
  </how-to-verify>
  <resume-signal>Type "approved" if all ACC-01, ACC-02, ACC-03 checks pass. Or describe specific failures to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard input → modal state | Keyboard events handled by focus trap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Denial of Service | Focus trap event listener | mitigate | Listener is cleaned up on useEffect return (modal unmount) — no memory leak |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
ACC-01: DayNightToggle keyboard operable (Space/Enter), WelcomeModal focus trap functional, Escape dismisses
ACC-02: ARIA attributes present on toggle (aria-pressed, French aria-label) and modal (role=dialog, aria-modal, labelledby, describedby), live regions in PortfolioGrid and AlbumsDragTrack
ACC-03: WelcomeModal buttons meet WCAG AA contrast, toggle icons meet contrast requirements
All: human checkpoint passed
</verification>

<success_criteria>
- All 13 automated grep assertions pass
- Human verifier approves keyboard navigation, ARIA structure, and contrast checks
- Build passes
</success_criteria>

<output>
Create `.planning/phases/04-polish-enhancements/04-06-SUMMARY.md` when done
</output>
