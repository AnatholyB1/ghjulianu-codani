---
phase: 04-polish-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-polish-enhancements/04-VIDEO-PROMPTS.md
  - public/transitions/day-to-night.mp4
  - public/transitions/night-to-day.mp4
autonomous: false
requirements:
  - ANI-01

must_haves:
  truths:
    - "Two MP4 video files exist at /public/transitions/ as day-to-night.mp4 and night-to-day.mp4"
    - "Both videos are photorealistic sky timelapses, ~2-3 seconds each, suitable for fullscreen web autoplay"
    - "Video prompts are documented in 04-VIDEO-PROMPTS.md for reproducibility"
  artifacts:
    - path: "public/transitions/day-to-night.mp4"
      provides: "Day→night sky timelapse for VideoTransitionOverlay and WelcomeModal background"
    - path: "public/transitions/night-to-day.mp4"
      provides: "Night→day sky timelapse for VideoTransitionOverlay"
    - path: ".planning/phases/04-polish-enhancements/04-VIDEO-PROMPTS.md"
      provides: "Documented prompts for reproducibility"
  key_links:
    - from: "VideoTransitionOverlay (plan 04-04)"
      to: "public/transitions/day-to-night.mp4"
      via: "src prop '/transitions/day-to-night.mp4'"
      pattern: "day-to-night.mp4"
    - from: "WelcomeModal (plan 04-05)"
      to: "public/transitions/day-to-night.mp4"
      via: "background video loop"
      pattern: "day-to-night.mp4"

user_setup: []
---

<objective>
Generate two AI sky timelapse videos via the Higgsfield MCP tool and save them as web-ready MP4 files in `/public/transitions/`. These assets gate Wave 2 plans (VideoTransitionOverlay, WelcomeModal) which reference the file paths.

Purpose: The day→night and night→day transition videos are the cinematic centerpiece of Phase 4. They play as fullscreen overlays on mode toggle and as the WelcomeModal background.
Output: Two MP4 files in /public/transitions/, one prompts documentation file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-polish-enhancements/04-CONTEXT.md
@.planning/phases/04-polish-enhancements/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document video prompts and create /public/transitions/ directory</name>
  <files>.planning/phases/04-polish-enhancements/04-VIDEO-PROMPTS.md</files>
  <read_first>
    - .planning/phases/04-polish-enhancements/04-CONTEXT.md — D-04 and D-05 for video asset requirements
    - .planning/phases/04-polish-enhancements/04-UI-SPEC.md — VideoTransitionOverlay section for usage context
  </read_first>
  <action>
    1. Create the `/public/transitions/` directory (run `mkdir -p public/transitions`).

    2. Create `.planning/phases/04-polish-enhancements/04-VIDEO-PROMPTS.md` documenting the exact generation prompts for both videos. The prompts are:

    **Prompt A — day-to-night.mp4:**
    "Photorealistic sky timelapse transitioning from bright golden hour afternoon to deep blue dusk and into early night. Pure sky only — no landscape, no horizon, no ground. Cinematic. Smooth, continuous motion. ~2-3 seconds. The sky begins as warm golden amber, gradually shifts through twilight purples and deep blues to a star-dotted dark navy. Ultra HD, no camera movement, stable composition."

    **Prompt B — night-to-day.mp4:**
    "Photorealistic sky timelapse transitioning from deep navy star-filled night sky to warm amber sunrise. Pure sky only — no landscape, no horizon, no ground. Cinematic. Smooth, continuous motion. ~2-3 seconds. The sky begins as deep navy with visible stars, gradually shifts through dark blue and pre-dawn grey to a warm amber and golden sunrise glow. Ultra HD, no camera movement, stable composition."

    Include in the doc: prompt text, target duration, target format (MP4 H.264), target resolution (minimum 1080p), intended use (fullscreen overlay with `autoPlay muted playsInline`), and output file paths.
  </action>
  <acceptance_criteria>
    - `.planning/phases/04-polish-enhancements/04-VIDEO-PROMPTS.md` exists and contains both prompt texts
    - `public/transitions/` directory exists (verify with `ls public/transitions/` or equivalent)
    - Both file paths (`public/transitions/day-to-night.mp4` and `public/transitions/night-to-day.mp4`) are listed in the doc as target outputs
  </acceptance_criteria>
  <verify>
    <automated>ls public/transitions/ 2>/dev/null && echo "directory exists" || echo "directory missing"</automated>
  </verify>
  <done>Prompts documented in 04-VIDEO-PROMPTS.md, /public/transitions/ directory created.</done>
</task>

<task type="auto">
  <name>Task 2: Authenticate with Higgsfield and generate both video assets</name>
  <files>public/transitions/day-to-night.mp4, public/transitions/night-to-day.mp4</files>
  <read_first>
    - .planning/phases/04-polish-enhancements/04-VIDEO-PROMPTS.md — the exact prompts just documented in Task 1
  </read_first>
  <action>
    1. Authenticate with the Higgsfield MCP tool using `mcp__higgsfield__authenticate`.

    2. Generate the day-to-night video using the exact Prompt A from 04-VIDEO-PROMPTS.md. Request parameters:
       - Prompt: the full text from 04-VIDEO-PROMPTS.md Prompt A
       - Duration: ~2-3 seconds
       - Format: MP4 / H.264
       - Resolution: 1080p minimum

    3. Save the generated video to `public/transitions/day-to-night.mp4`.

    4. Generate the night-to-day video using the exact Prompt B from 04-VIDEO-PROMPTS.md. Same parameters.

    5. Save the generated video to `public/transitions/night-to-day.mp4`.

    If the Higgsfield MCP tool returns a download URL rather than binary content, use a fetch/curl to download to the correct path. If the tool is unavailable or returns an error, document the failure in 04-VIDEO-PROMPTS.md under a "Generation Status" section and create placeholder files at the expected paths so Wave 2 plans can proceed (the placeholders can be empty or minimal valid MP4 stubs — Wave 2 components reference the path but do not require the video to play during development).
  </action>
  <acceptance_criteria>
    - `public/transitions/day-to-night.mp4` exists (non-zero file size if generation succeeded)
    - `public/transitions/night-to-day.mp4` exists (non-zero file size if generation succeeded)
    - If generation failed, 04-VIDEO-PROMPTS.md contains a "Generation Status: FAILED" section with the error, AND placeholder files at the correct paths exist
    - `ls -la public/transitions/` shows both files
  </acceptance_criteria>
  <verify>
    <automated>ls -la public/transitions/*.mp4 2>/dev/null || echo "MP4 files not yet present"</automated>
  </verify>
  <done>Both MP4 files exist at public/transitions/. If AI generation failed, placeholders are present with documented status.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Generated two AI sky timelapse MP4 videos:
    - public/transitions/day-to-night.mp4 — golden hour to dark blue dusk sky
    - public/transitions/night-to-day.mp4 — navy star night to warm amber sunrise sky
  </what-built>
  <how-to-verify>
    1. Open public/transitions/day-to-night.mp4 in a video player (VLC, browser, or Finder/Explorer preview)
    2. Confirm: pure sky timelapse, day-to-night direction, no landscape/horizon visible, ~2-3 seconds duration
    3. Open public/transitions/night-to-day.mp4 in a video player
    4. Confirm: pure sky timelapse, night-to-day direction, no landscape/horizon visible, ~2-3 seconds duration
    5. Confirm both play smoothly without artifacts

    If either video does not match the brief (wrong direction, landscape visible, too long, wrong mood), report the issue and describe what was generated so the prompts can be refined.

    If videos are placeholder stubs (Higgsfield unavailable), type "placeholder — proceed" to allow Wave 2 plans to continue with the understanding that real videos will replace the stubs before production deployment.
  </how-to-verify>
  <resume-signal>Type "approved" if both videos match the brief, or describe issues / "placeholder — proceed"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Higgsfield MCP → filesystem | Generated video content written to /public/ (served statically) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | MP4 files in /public/transitions/ | accept | Static assets; no user-supplied content; no executable risk |
| T-04-04 | Information Disclosure | Higgsfield API authentication | accept | Auth token is ephemeral MCP session; not persisted to disk |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
1. `ls -la public/transitions/` shows both `day-to-night.mp4` and `night-to-day.mp4`
2. Both files have non-zero file sizes (or are documented as placeholders)
3. Human checkpoint passed — video content verified or placeholder acknowledged
4. `.planning/phases/04-polish-enhancements/04-VIDEO-PROMPTS.md` contains both prompt texts and generation status
</verification>

<success_criteria>
- public/transitions/day-to-night.mp4 exists and is a playable sky timelapse (day→night, ~2-3s, pure sky)
- public/transitions/night-to-day.mp4 exists and is a playable sky timelapse (night→day, ~2-3s, pure sky)
- Video prompts documented in 04-VIDEO-PROMPTS.md
- Human reviewer has verified or acknowledged placeholder status
</success_criteria>

<output>
Create `.planning/phases/04-polish-enhancements/04-02-SUMMARY.md` when done
</output>
