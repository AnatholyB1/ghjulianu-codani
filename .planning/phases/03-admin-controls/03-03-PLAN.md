---
phase: 03-admin-controls
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/app/admin/_components/DayNightToggleBadge.tsx
  - src/app/admin/portfolio/_components/PortfolioAdminGrid.tsx
  - src/app/admin/portfolio/page.tsx
autonomous: true
requirements: [PHO-01, PHO-02, PHO-03]
must_haves:
  truths:
    - "Each portfolio photo card shows a Sun/Moon badge reflecting its is_day value (per D-04)"
    - "Clicking the badge cycles NULL->true->false->NULL with optimistic update (per D-02)"
    - "SELECT mode shows checkboxes on each card and a bulk action bar (per D-06)"
    - "Bulk JOUR/NUIT/NON TAGGE buttons update all selected photos in one Server Action call (per D-08)"
    - "Portfolio admin page is a thin Server Component shell passing initialPhotos to PortfolioAdminGrid (per D-07)"
  artifacts:
    - path: "src/app/admin/_components/DayNightToggleBadge.tsx"
      provides: "Reusable 3-state cycle badge component"
      exports: ["default"]
    - path: "src/app/admin/portfolio/_components/PortfolioAdminGrid.tsx"
      provides: "Client Component with selection mode and bulk actions"
      exports: ["default"]
    - path: "src/app/admin/portfolio/page.tsx"
      provides: "Thin server shell rendering PortfolioAdminGrid"
  key_links:
    - from: "src/app/admin/_components/DayNightToggleBadge.tsx"
      to: "src/app/admin/actions.ts"
      via: "import updatePortfolioPhotoDay"
      pattern: "import.*updatePortfolioPhotoDay.*actions"
    - from: "src/app/admin/portfolio/_components/PortfolioAdminGrid.tsx"
      to: "src/app/admin/actions.ts"
      via: "import bulkUpdatePortfolioPhotoDay and deletePortfolioPhoto"
      pattern: "import.*bulkUpdatePortfolioPhotoDay.*actions"
    - from: "src/app/admin/portfolio/page.tsx"
      to: "src/app/admin/portfolio/_components/PortfolioAdminGrid.tsx"
      via: "initialPhotos prop"
      pattern: "PortfolioAdminGrid.*initialPhotos"
---

<objective>
Build the portfolio admin grid with inline day/night toggle and bulk selection mode.

This plan creates the two new Client Components (DayNightToggleBadge and PortfolioAdminGrid) and converts the portfolio page to a thin shell. Implements D-01 (inline toggle), D-02 (three-state cycle), D-04 (badge indicators), D-06 (SELECT mode), D-07 (extracted Client Component), D-08 (bulk Server Action).

Purpose: Enable admin to tag individual and bulk portfolio photos as day/night/untagged
Output: Working portfolio admin page with inline toggles and bulk selection
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-controls/03-RESEARCH.md
@.planning/phases/03-admin-controls/03-PATTERNS.md
@.planning/phases/03-admin-controls/03-UI-SPEC.md
@.planning/phases/03-admin-controls/03-01-SUMMARY.md
@.planning/phases/03-admin-controls/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DayNightToggleBadge Client Component</name>
  <files>src/app/admin/_components/DayNightToggleBadge.tsx</files>
  <read_first>
    - src/app/admin/_components/ConfirmButton.tsx (primary analog — useTransition + disabled pending pattern)
    - src/app/admin/actions.ts (verify updatePortfolioPhotoDay export signature)
    - .planning/phases/03-admin-controls/03-UI-SPEC.md (Section 1: DayNightToggleBadge interaction contract — badge pill styles, state machine, accessibility labels)
    - .planning/phases/03-admin-controls/03-PATTERNS.md (DayNightToggleBadge section — exact import pattern, props, state, core handler, inline style constants)
  </read_first>
  <action>
    Create src/app/admin/_components/DayNightToggleBadge.tsx as a 'use client' component.

    Props: { photoId: string, isDay: boolean | null }. Component renders a button element that cycles through three states per D-02: null -> true -> false -> null.

    State: useState for value (boolean | null) initialized from isDay prop. useTransition for isPending/startTransition.

    Three-state cycle function nextIsDay: if current === null return true, if current === true return false, if current === false return null.

    Click handler: compute next value, optimistic setValue(next), then startTransition calling updatePortfolioPhotoDay(photoId, next). On catch, revert setValue back to previous value.

    Render logic per UI-SPEC Section 1:
    - value === null: render a button with btnNull style (transparent 18x18px click target, no visible pill)
    - value === true: render button with pillDay style (amber background rgba(200,169,126,0.15), border rgba(200,169,126,0.35)), containing Sun icon from lucide-react size={10} color='#c8a97e'
    - value === false: render button with pillNight style (background rgba(120,140,180,0.15), border rgba(120,140,180,0.35)), containing Moon icon size={10} color='#8090b0'

    Pending state: apply opacity 0.5, pointerEvents 'none' to button.

    Accessibility per UI-SPEC: aria-label 'Marquer comme jour' when null, 'Marquer comme nuit' when true, 'Retirer le marquage' when false.

    Import Sun, Moon from lucide-react. Import updatePortfolioPhotoDay from '@/app/admin/actions'.

    Define named React.CSSProperties constants at bottom of file: btnNull, pillDay, pillNight. All values from PATTERNS.md DayNightToggleBadge section. Width/height 18px, borderRadius 2px, padding 2px 4px.

    NOTE: This component renders the button only. The caller (PortfolioAdminGrid) wraps it in a positioned div at top:'3px', right:'26px'.
  </action>
  <verify>
    <automated>rtk tsc</automated>
  </verify>
  <acceptance_criteria>
    - DayNightToggleBadge.tsx exists with 'use client' directive
    - Exports default function with props { photoId: string, isDay: boolean | null }
    - Contains nextIsDay cycle logic: null->true->false->null
    - Uses useState + useTransition for optimistic update pattern
    - Imports Sun, Moon from lucide-react
    - Imports updatePortfolioPhotoDay from '@/app/admin/actions'
    - Three named style constants at bottom: btnNull, pillDay, pillNight
    - aria-label attributes differ by state (three French labels)
    - Pending state applies opacity 0.5 and pointerEvents 'none'
    - rtk tsc exits 0
  </acceptance_criteria>
  <done>DayNightToggleBadge renders three-state badge with optimistic Server Action calls</done>
</task>

<task type="auto">
  <name>Task 2: Create PortfolioAdminGrid Client Component and convert page to thin shell</name>
  <files>src/app/admin/portfolio/_components/PortfolioAdminGrid.tsx, src/app/admin/portfolio/page.tsx</files>
  <read_first>
    - src/app/admin/portfolio/page.tsx (current full page — understand grid layout, card structure, ConfirmButton usage, style constants to migrate)
    - src/app/admin/albums/_components/AlbumSortableList.tsx (primary analog — 'use client' Client Component receiving initialAlbums prop, useTransition, state management)
    - src/app/admin/_components/DraggablePhotoGrid.tsx (secondary analog — grid layout, card structure with position relative, per-card absolute-positioned buttons)
    - src/app/admin/actions.ts (verify bulkUpdatePortfolioPhotoDay and deletePortfolioPhoto signatures)
    - .planning/phases/03-admin-controls/03-UI-SPEC.md (Sections 2+3: PortfolioAdminGrid normal mode and bulk-select mode interaction contracts)
    - .planning/phases/03-admin-controls/03-PATTERNS.md (PortfolioAdminGrid section — exact imports, props, state, bulk handler, grid layout, card structure, style constants)
  </read_first>
  <action>
    PART A: Create src/app/admin/portfolio/_components/PortfolioAdminGrid.tsx (ensure _components directory exists).

    'use client' component. Props: { initialPhotos: PortfolioPhoto[] } per D-07. Import PortfolioPhoto from '@/lib/db.types'. Import ConfirmButton from '../../_components/ConfirmButton'. Import DayNightToggleBadge from '../../_components/DayNightToggleBadge'. Import deletePortfolioPhoto and bulkUpdatePortfolioPhotoDay from '../../actions'.

    State: photos (PortfolioPhoto[]) from initialPhotos, selectionMode (boolean false), selectedIds (Set of string), bulkPending via useTransition.

    Selection mode toggle: button above grid labeled 'SELECTIONNER' (inactive) / 'ANNULER' (active). On cancel, clear selectedIds and set selectionMode false.

    Bulk action bar per D-06: rendered only when selectionMode is true. Contains three buttons: JOUR (fires handleBulkUpdate(true)), NUIT (fires handleBulkUpdate(false)), NON TAGGE (fires handleBulkUpdate(null)). Counter text at right: '{selectedIds.size} selectionne(s)'. Buttons disabled when selectedIds.size === 0 (opacity 0.4, pointerEvents none). During bulk pending: clicked button text becomes 'MISE A JOUR...', all buttons disabled.

    handleBulkUpdate(isDay): convert selectedIds to array, if empty return. Optimistic update: setPhotos updating is_day for matching ids. startBulkTrans calling bulkUpdatePortfolioPhotoDay(ids, isDay). On success: clear selectedIds, exit selectionMode. On error: revert photos state, keep selectionMode active, show inline error text 'Erreur -- reessayez.' per RESEARCH.md Pitfall 7.

    Photo grid: display grid, gridTemplateColumns repeat(auto-fill,minmax(140px,1fr)), gap 4px.

    Per card structure (position relative, background #1a1a1a):
    - img: full bleed, aspectRatio 1, objectFit cover, display block
    - When NOT in selectionMode: DayNightToggleBadge in positioned div at position absolute top 3px right 26px. ConfirmButton for delete at position absolute top 3px right 3px.
    - When IN selectionMode: checkbox overlay at top 4px left 4px (14x14px, border 1px solid rgba(255,255,255,0.3), background #080808). Selected state: checkbox filled with #c8a97e, card outline 2px solid rgba(200,169,126,0.5), card bg tint rgba(200,169,126,0.08). Hide DayNightToggleBadge and ConfirmButton (pointerEvents none, opacity 0 or do not render).
    - Clicking card in selectionMode toggles its id in selectedIds set.

    Style constants at bottom of file per PATTERNS.md PortfolioAdminGrid section: btnSelectMode, btnSelectModeActive, bulkBar, btnBulkDay, btnBulkNight, btnBulkUntagged, bulkCounter, cardTitle. All exact values from PATTERNS.md/UI-SPEC.

    PART B: Modify src/app/admin/portfolio/page.tsx to thin shell per D-07.

    Add import: import PortfolioAdminGrid from './_components/PortfolioAdminGrid'.

    Keep the existing Supabase fetch query (select('*').order('sort_order').order('created_at')) — it already includes is_day via select('*').

    Replace the inline grid div (the photos.map loop with ConfirmButton per card) with: PortfolioAdminGrid initialPhotos={photos ?? []}. Keep AddPortfolioPhotoForm import and rendering unchanged. Keep pageTitle, subLabel, card, cardTitle style constants used by the remaining page header section. Remove style constants only used by the old inline grid (formCol, labelS, inputS, btnPrimary if they exist and are unused).

    Remove the ConfirmButton import from page.tsx if no longer used directly (it moved to PortfolioAdminGrid).
  </action>
  <verify>
    <automated>rtk tsc</automated>
  </verify>
  <acceptance_criteria>
    - src/app/admin/portfolio/_components/PortfolioAdminGrid.tsx exists with 'use client' directive
    - PortfolioAdminGrid accepts { initialPhotos: PortfolioPhoto[] } prop
    - Contains selectionMode state, selectedIds Set state, bulkPending useTransition
    - SELECTIONNER/ANNULER toggle button renders above grid
    - Bulk action bar with JOUR, NUIT, NON TAGGE buttons renders when selectionMode true
    - Counter shows selectedIds.size + ' selectionne(s)'
    - Grid uses repeat(auto-fill,minmax(140px,1fr)) with gap 4px
    - Each card renders DayNightToggleBadge at top 3px right 26px and ConfirmButton at top 3px right 3px in normal mode
    - Selection mode shows checkboxes, hides badge and delete, highlights selected cards
    - handleBulkUpdate calls bulkUpdatePortfolioPhotoDay, exits selectionMode on success, keeps it on error
    - Style constants defined at bottom of file matching UI-SPEC values
    - portfolio/page.tsx renders PortfolioAdminGrid initialPhotos={photos ?? []}
    - portfolio/page.tsx no longer contains the photos.map loop
    - rtk tsc exits 0
  </acceptance_criteria>
  <done>Portfolio admin page converted to thin shell + PortfolioAdminGrid with inline toggle and bulk selection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click -> Server Action | Badge click and bulk action trigger server-side writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | DayNightToggleBadge cycle | mitigate | nextIsDay returns only true/false/null — no arbitrary values possible |
| T-03-07 | Tampering | Bulk selection ids | mitigate | ids come from selectedIds Set populated only from photo.id values in initialPhotos prop; UUID validation in bulkUpdatePortfolioPhotoDay Server Action (Plan 02) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- Portfolio admin page loads and renders PortfolioAdminGrid
- Each photo card shows correct badge state
- Clicking badge cycles through null -> day -> night -> null
- SELECT mode shows checkboxes and bulk action bar
- Bulk actions update selected photos
- rtk tsc and rtk next build pass
</verification>

<success_criteria>
Portfolio admin page is a thin shell with PortfolioAdminGrid Client Component. Individual inline toggle and bulk selection mode both functional. DayNightToggleBadge reusable component works with optimistic updates.
</success_criteria>

<output>
Create `.planning/phases/03-admin-controls/03-03-SUMMARY.md` when done
</output>
