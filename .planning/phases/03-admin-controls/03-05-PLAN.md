---
phase: 03-admin-controls
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-04]
files_modified:
  - src/app/admin/_components/DraggablePhotoGrid.tsx
autonomous: true
requirements: [ALB-02, PHO-03]
must_haves:
  truths:
    - "Each album photo card shows an inherited badge when photo.is_day is null but album.is_day is set (per D-11)"
    - "Each album photo card shows an explicit override badge when photo.is_day is not null (per D-11)"
    - "No badge shown when both photo.is_day and album.is_day are null"
    - "Clicking inherited badge sets explicit override to opposite of album value (per D-11)"
    - "Clicking explicit override badge shows ALBUM reset label then fires Server Action to set is_day=NULL (per D-13)"
    - "Inheritance is app-level: album.is_day takes precedence, no DB computed columns (per D-12)"
  artifacts:
    - path: "src/app/admin/_components/DraggablePhotoGrid.tsx"
      provides: "Inheritance badge UI with inherited vs explicit states"
  key_links:
    - from: "src/app/admin/_components/DraggablePhotoGrid.tsx"
      to: "src/app/admin/actions.ts"
      via: "import updateAlbumPhotoDay"
      pattern: "import.*updateAlbumPhotoDay.*actions"
    - from: "src/app/admin/_components/DraggablePhotoGrid.tsx"
      to: "src/app/admin/albums/[id]/page.tsx"
      via: "albumIsDay prop received from parent"
      pattern: "albumIsDay"
---

<objective>
Implement the ALB-02 inheritance badge UI in DraggablePhotoGrid.

Each album photo card displays an inherited (muted icon + superscript 1) or explicit override (bright icon + !) badge at the bottom-right corner. Clicking an inherited badge sets an explicit override. Clicking an explicit override badge triggers a "reset to album" flow (D-13). This is the most interaction-dense component in Phase 3.

Purpose: Enable admin to see and manage per-album-photo is_day overrides with clear inherited vs explicit visual distinction
Output: Working inheritance badge system in album photo grid
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-controls/03-RESEARCH.md
@.planning/phases/03-admin-controls/03-PATTERNS.md
@.planning/phases/03-admin-controls/03-UI-SPEC.md
@.planning/phases/03-admin-controls/03-01-SUMMARY.md
@.planning/phases/03-admin-controls/03-02-SUMMARY.md
@.planning/phases/03-admin-controls/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inheritance badge logic and Server Action wiring to DraggablePhotoGrid</name>
  <files>src/app/admin/_components/DraggablePhotoGrid.tsx</files>
  <read_first>
    - src/app/admin/_components/DraggablePhotoGrid.tsx (full file — current Props interface, existing useTransition at line 25, card render loop, delete button position at top 3px right 3px, existing style constants at bottom)
    - src/app/admin/actions.ts (verify updateAlbumPhotoDay(id: string, albumId: string, isDay: boolean | null) signature)
    - src/lib/db.types.ts (verify AlbumPhoto now has is_day: boolean | null after Plan 01)
    - .planning/phases/03-admin-controls/03-PATTERNS.md (DraggablePhotoGrid section — Props modification, import additions, badge decision logic, badge styles)
    - .planning/phases/03-admin-controls/03-UI-SPEC.md (Section 6: DraggablePhotoGrid Inheritance Badge — badge variants table, click behavior, reset-to-album flow)
  </read_first>
  <action>
    Modify src/app/admin/_components/DraggablePhotoGrid.tsx to implement D-11/D-12/D-13.

    IMPORTS: Add import { Sun, Moon } from 'lucide-react'. Add import { updateAlbumPhotoDay } from '../actions'.

    PROPS: The albumIsDay?: boolean | null field should already be in Props from Plan 04. If not, add it. Destructure albumIsDay from props.

    STATE: Add a second useTransition: const [badgePending, startBadgeTrans] = useTransition(). Add state for reset animation: const [resetPhotoId, setResetPhotoId] = useState of string or null, initialized to null.

    BADGE DECISION FUNCTION (per RESEARCH.md Code Examples):
    getBadgeState(photoIsDay: boolean | null, albumIsDay: boolean | null): returns null (no badge), or { type: 'inherited', value: boolean } or { type: 'explicit', value: boolean }.
    Logic: if photoIsDay !== null return { type: 'explicit', value: photoIsDay }. Else if albumIsDay !== null return { type: 'inherited', value: albumIsDay }. Else return null.

    BADGE RENDER (inside each photo card div, AFTER existing content): Compute badgeState = getBadgeState(photo.is_day, albumIsDay). If badgeState is null, render nothing.

    If badgeState.type === 'inherited':
    - Render a button with badgeInherited style at position absolute bottom 3px right 3px
    - Contents: Sun or Moon icon (size 10) with muted color (rgba(200,169,126,0.5) for day, rgba(120,140,180,0.5) for night) + span with superscript text '1' at fontSize 0.58rem
    - onClick: set explicit override to OPPOSITE of album value. If albumIsDay true, set photo to false (explicit night). If albumIsDay false, set photo to true (explicit day). Call handleBadgeClick(photo.id, oppositeValue).

    If badgeState.type === 'explicit' AND resetPhotoId !== photo.id:
    - Render a button with badgeDay or badgeNight style (depending on badgeState.value)
    - Contents: Sun or Moon icon (size 10) with bright color (#c8a97e or #8090b0) + span with text '!' at fontSize 0.58rem
    - onClick: start reset flow. Set setResetPhotoId(photo.id). After 1500ms timeout, call handleResetToAlbum(photo.id). Per D-13 and UI-SPEC Section 6.

    If badgeState.type === 'explicit' AND resetPhotoId === photo.id:
    - Render a button with badgeReset style showing text 'ALBUM reset-arrow' in color #e07070, fontSize 0.58rem
    - This is the 1500ms confirmation display before the reset fires

    HANDLER FUNCTIONS:
    handleBadgeClick(photoId: string, newValue: boolean | null): optimistic local state update on photos array (set photo.is_day = newValue for matching id). startBadgeTrans calling updateAlbumPhotoDay(photoId, albumId, newValue). On catch, revert.

    handleResetToAlbum(photoId: string): after the 1500ms visual delay, call handleBadgeClick(photoId, null) to set is_day back to NULL (inherits from album again per D-13). Clear resetPhotoId.

    PENDING STATE: if badgePending, apply opacity 0.5, pointerEvents none to badge buttons.

    STYLE CONSTANTS (append after existing hint constant at bottom of file):
    - badgeDay: position absolute, bottom 3px, right 3px, background rgba(200,169,126,0.15), border 1px solid rgba(200,169,126,0.35), borderRadius 2px, padding 2px 4px, fontSize 0.58rem, display flex, alignItems center, gap 0.25rem, cursor pointer
    - badgeNight: spread badgeDay, background rgba(120,140,180,0.15), border 1px solid rgba(120,140,180,0.35)
    - badgeInherited: position absolute, bottom 3px, right 3px, background transparent, border 1px solid rgba(255,255,255,0.06), borderRadius 2px, padding 2px 4px, fontSize 0.58rem, display flex, alignItems center, gap 0.25rem, cursor pointer, opacity 0.7
    - badgeReset: spread badgeInherited, color #e07070, opacity 1

    All values from PATTERNS.md DraggablePhotoGrid section and UI-SPEC Section 6.
  </action>
  <verify>
    <automated>rtk tsc</automated>
  </verify>
  <acceptance_criteria>
    - DraggablePhotoGrid.tsx imports Sun, Moon from lucide-react
    - DraggablePhotoGrid.tsx imports updateAlbumPhotoDay from '../actions'
    - Props interface includes albumIsDay?: boolean | null
    - getBadgeState function returns correct badge type for all 5 combinations in UI-SPEC table
    - Inherited badge renders muted icon + superscript 1 at bottom 3px right 3px
    - Explicit badge renders bright icon + ! marker
    - Clicking inherited badge calls updateAlbumPhotoDay with opposite of album value
    - Clicking explicit badge shows 'ALBUM reset-arrow' in #e07070 for 1500ms then fires updateAlbumPhotoDay with null
    - Badge position (bottom 3px, right 3px) does NOT conflict with delete button (top 3px, right 3px)
    - Four new style constants at bottom: badgeDay, badgeNight, badgeInherited, badgeReset
    - Second useTransition (badgePending) used for badge Server Action calls
    - Optimistic local state update on photos array for badge changes
    - rtk tsc exits 0
  </acceptance_criteria>
  <done>DraggablePhotoGrid shows inherited vs explicit badges with full override and reset-to-album functionality</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Badge click -> Server Action | Override/reset action crosses into updateAlbumPhotoDay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | Badge click value | mitigate | Only true/false/null passed to updateAlbumPhotoDay; values computed from getBadgeState logic, not user input |
| T-03-10 | Repudiation | Reset-to-album delay | accept | 1500ms delay provides visual confirmation; no persistent undo needed for low-risk tag change |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- Album edit page shows badges on photo cards
- Inherited photos show muted icon + superscript 1
- Explicit override photos show bright icon + !
- Click inherited -> explicit override appears
- Click explicit -> ALBUM reset label shows, then reverts to inherited
- Both album and photo is_day null -> no badge
- rtk tsc passes
- rtk next build passes
</verification>

<success_criteria>
Full ALB-02 inheritance UI working: inherited vs explicit badges render correctly, overrides persist via Server Action, reset-to-album clears override back to NULL. All interaction patterns match UI-SPEC Section 6.
</success_criteria>

<output>
Create `.planning/phases/03-admin-controls/03-05-SUMMARY.md` when done
</output>
