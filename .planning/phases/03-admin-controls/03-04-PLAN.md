---
phase: 03-admin-controls
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - src/app/admin/albums/_components/AlbumSortableList.tsx
  - src/app/admin/albums/[id]/page.tsx
  - src/app/admin/_components/DraggablePhotoGrid.tsx
autonomous: true
requirements: [ALB-01, ALB-03]
must_haves:
  truths:
    - "Each album row in AlbumSortableList shows a Sun/Moon/dash indicator for its is_day value (per D-05)"
    - "Album edit form has a MODE JOUR/NUIT tri-state radio group (per D-09/D-10)"
    - "Saving the album edit form persists is_day value via updateAlbum Server Action"
    - "DraggablePhotoGrid receives albumIsDay prop from the album edit page"
  artifacts:
    - path: "src/app/admin/albums/_components/AlbumSortableList.tsx"
      provides: "Day/night indicator per album row"
    - path: "src/app/admin/albums/[id]/page.tsx"
      provides: "MODE JOUR/NUIT radio group + albumIsDay prop pass-through"
  key_links:
    - from: "src/app/admin/albums/[id]/page.tsx"
      to: "src/app/admin/_components/DraggablePhotoGrid.tsx"
      via: "albumIsDay prop"
      pattern: "albumIsDay.*album\\.is_day"
    - from: "src/app/admin/albums/[id]/page.tsx"
      to: "src/app/admin/actions.ts"
      via: "updateAlbum form action reads is_day from formData"
      pattern: "name.*is_day"
---

<objective>
Add day/night indicators to album list rows and tri-state radio to album edit form.

Implements D-05 (album row indicators in AlbumSortableList), D-09/D-10 (album edit form MODE JOUR/NUIT radio), and passes albumIsDay prop to DraggablePhotoGrid for Plan 05's inheritance UI.

Purpose: Enable admin to see and set album-level day/night status
Output: Album list shows indicators, album edit form saves is_day
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-controls/03-RESEARCH.md
@.planning/phases/03-admin-controls/03-PATTERNS.md
@.planning/phases/03-admin-controls/03-UI-SPEC.md
@.planning/phases/03-admin-controls/03-01-SUMMARY.md
@.planning/phases/03-admin-controls/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add day/night indicator to AlbumSortableList rows</name>
  <files>src/app/admin/albums/_components/AlbumSortableList.tsx</files>
  <read_first>
    - src/app/admin/albums/_components/AlbumSortableList.tsx (full file — understand row render structure, tag spans at lines 123-129, existing tag style constant at line 172, import block)
    - .planning/phases/03-admin-controls/03-PATTERNS.md (AlbumSortableList section — exact insertion point, three render states, import addition)
    - .planning/phases/03-admin-controls/03-UI-SPEC.md (Section 4: AlbumSortableList Day/Night Indicator per Row — styles and icon sizes)
  </read_first>
  <action>
    Modify src/app/admin/albums/_components/AlbumSortableList.tsx per D-05.

    Add import: import { Sun, Moon } from 'lucide-react' at the top of the file after existing imports.

    In the row render, inside the tags div (the div with display flex, gap 0.8rem, flexWrap wrap), insert a new span AFTER the is_public span (the one showing PUBLIC/PRIVE) and BEFORE the sort_order span (showing #{album.sort_order}).

    Three conditional renders per PATTERNS.md:
    - album.is_day === true: span with style { ...tag, color: '#c8a97e', display: 'flex', alignItems: 'center' } containing Sun size={9}
    - album.is_day === false: span with style { ...tag, color: '#8090b0', display: 'flex', alignItems: 'center' } containing Moon size={9}
    - album.is_day === null: span with style { ...tag, color: 'rgba(122,122,116,0.4)' } containing the text dash character

    No new style constants needed — reuse existing tag constant with inline color overrides. No interactivity — read-only indicator per UI-SPEC Section 4.

    The Album type already has is_day: boolean | null (confirmed in db.types.ts), so AlbumRow (which extends Album) already includes it. No type changes needed.
  </action>
  <verify>
    <automated>rtk tsc</automated>
  </verify>
  <acceptance_criteria>
    - AlbumSortableList.tsx imports Sun, Moon from lucide-react
    - Row render contains three conditional spans for is_day true/false/null between is_public and sort_order spans
    - is_day true renders Sun size={9} in color #c8a97e
    - is_day false renders Moon size={9} in color #8090b0
    - is_day null renders dash in color rgba(122,122,116,0.4)
    - No new state or event handlers added (read-only)
    - rtk tsc exits 0
  </acceptance_criteria>
  <done>Album list rows show day/night/untagged indicator</done>
</task>

<task type="auto">
  <name>Task 2: Add MODE JOUR/NUIT radio and albumIsDay prop to album edit page</name>
  <files>src/app/admin/albums/[id]/page.tsx, src/app/admin/_components/DraggablePhotoGrid.tsx</files>
  <read_first>
    - src/app/admin/albums/[id]/page.tsx (full file — understand form structure, VISIBILITE radio pattern at lines 88-97, radioLabel constant at line 148, DraggablePhotoGrid call at line 122, Row component usage)
    - src/app/admin/_components/DraggablePhotoGrid.tsx (lines 1-15 — Props interface at lines 8-11)
    - .planning/phases/03-admin-controls/03-PATTERNS.md (albums/[id]/page.tsx section — exact radio row, radioLabelSm constant, DraggablePhotoGrid call change)
    - .planning/phases/03-admin-controls/03-UI-SPEC.md (Section 5: Album Edit Form MODE JOUR/NUIT — radio layout, icon usage, defaultChecked logic)
  </read_first>
  <action>
    Modify src/app/admin/albums/[id]/page.tsx per D-09/D-10.

    Add import: import { Sun, Moon } from 'lucide-react' after existing imports.

    Add new form row after the VISIBILITE Row block (after line 98, before the ENREGISTRER button). Use the existing Row component with label 'MODE JOUR/NUIT'. Inside, render a div with style display flex, gap 1.5rem containing three radio labels:

    1. Label with style radioLabelSm: input type='radio' name='is_day' value='true' defaultChecked={album.is_day === true}, Sun size={10} style={{ color: '#c8a97e' }}, span text 'JOUR'
    2. Label with style radioLabelSm: input type='radio' name='is_day' value='false' defaultChecked={album.is_day === false}, Moon size={10} style={{ color: '#8090b0' }}, span text 'NUIT'
    3. Label with style radioLabelSm: input type='radio' name='is_day' value='' defaultChecked={album.is_day === null}, span with style color '#7a7a74' text dash, span text 'NON TAGGE'

    CRITICAL per RESEARCH.md Open Question 1: define a NEW radioLabelSm constant at the bottom of the file (do NOT modify existing radioLabel). radioLabelSm: { display: 'flex', alignItems: 'center', gap: '0.5rem', fontSize: '0.62rem', color: '#E8E4DC', cursor: 'pointer', letterSpacing: '0.1em' }. This prevents visual regression on existing VISIBILITE radios which use radioLabel at fontSize 0.7rem.

    Modify the DraggablePhotoGrid call (line 122) to pass the albumIsDay prop: DraggablePhotoGrid photos={photos ?? []} albumId={id} albumIsDay={album.is_day}.

    Also update DraggablePhotoGrid.tsx: add albumIsDay?: boolean | null to the Props interface (lines 8-11). This is a one-line addition that prepares for Plan 05's larger modifications. Plan 05 will find the field already present and use it.
  </action>
  <verify>
    <automated>rtk tsc</automated>
  </verify>
  <acceptance_criteria>
    - albums/[id]/page.tsx imports Sun, Moon from lucide-react
    - A Row with label 'MODE JOUR/NUIT' exists in the form, containing three radio inputs with name='is_day'
    - Radio values are 'true', 'false', '' (empty string for NULL)
    - defaultChecked logic: album.is_day === true for JOUR, album.is_day === false for NUIT, album.is_day === null for NON TAGGE
    - radioLabelSm constant defined at bottom with fontSize '0.62rem' (separate from existing radioLabel at 0.7rem)
    - Existing radioLabel constant at line 148 is UNCHANGED
    - DraggablePhotoGrid call includes albumIsDay={album.is_day}
    - DraggablePhotoGrid Props interface in DraggablePhotoGrid.tsx includes albumIsDay?: boolean | null
    - rtk tsc exits 0
  </acceptance_criteria>
  <done>Album edit form has MODE JOUR/NUIT tri-state radio, albumIsDay passed to DraggablePhotoGrid, TypeScript compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML form -> Server Action | Radio input value crosses into updateAlbum Server Action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | is_day radio value | mitigate | updateAlbum (Plan 02) whitelist-parses formData: only 'true'/'false' accepted, all else -> null |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- Album list rows display Sun/Moon/dash indicator
- Album edit form shows MODE JOUR/NUIT radios with correct defaultChecked
- Saving form persists is_day to database (via updateAlbum from Plan 02)
- DraggablePhotoGrid receives albumIsDay prop
- rtk tsc passes
</verification>

<success_criteria>
Album admin list shows day/night indicators per row. Album edit form has working tri-state radio for is_day. DraggablePhotoGrid receives albumIsDay prop for inheritance badge in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/03-admin-controls/03-04-SUMMARY.md` when done
</output>
