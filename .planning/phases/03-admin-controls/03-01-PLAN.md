---
phase: 03-admin-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240626000007_add_album_photos_is_day.sql
  - src/lib/db.types.ts
autonomous: true
requirements: [ALB-02]
must_haves:
  truths:
    - "album_photos table has is_day boolean column in the live database"
    - "AlbumPhoto TypeScript interface includes is_day: boolean | null"
    - "TypeScript compilation succeeds with the updated AlbumPhoto type"
  artifacts:
    - path: "supabase/migrations/20240626000007_add_album_photos_is_day.sql"
      provides: "ALTER TABLE album_photos ADD COLUMN is_day"
      contains: "ALTER TABLE album_photos"
    - path: "src/lib/db.types.ts"
      provides: "AlbumPhoto type with is_day field"
      contains: "is_day"
  key_links:
    - from: "supabase/migrations/20240626000007_add_album_photos_is_day.sql"
      to: "src/lib/db.types.ts"
      via: "manual type sync"
      pattern: "is_day.*boolean"
---

<objective>
[BLOCKING] Add is_day column to album_photos table and update the AlbumPhoto TypeScript type.

This is the prerequisite for all Phase 3 features. The album_photos table currently lacks an is_day column (confirmed: not present in any of the 6 existing migrations, not in AlbumPhoto interface). D-11/D-12/D-13 require per-album-photo is_day overrides for the inheritance system (ALB-02).

Purpose: Unblock all downstream plans that write or read album_photos.is_day
Output: Migration file applied to Supabase, AlbumPhoto type updated in db.types.ts
</objective>

<execution_context>
@C:/Users/Anatholy/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Anatholy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-controls/03-RESEARCH.md
@.planning/phases/03-admin-controls/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create album_photos is_day migration and update AlbumPhoto type</name>
  <files>supabase/migrations/20240626000007_add_album_photos_is_day.sql, src/lib/db.types.ts</files>
  <read_first>
    - supabase/migrations/20240626000001_add_is_day_to_photos.sql (analog for SQL style and IF NOT EXISTS pattern)
    - src/lib/db.types.ts (current AlbumPhoto interface at lines 31-40 — field to add before sort_order)
  </read_first>
  <action>
    Create migration file supabase/migrations/20240626000007_add_album_photos_is_day.sql with a single ALTER TABLE statement: ALTER TABLE album_photos ADD COLUMN IF NOT EXISTS is_day BOOLEAN DEFAULT NULL. Use IF NOT EXISTS for idempotency (per assumption A3 in RESEARCH.md).

    Edit src/lib/db.types.ts AlbumPhoto interface (lines 31-40): insert is_day: boolean | null between the alt field and the sort_order field. This matches the field position used by PortfolioPhoto (line 48) and Album (line 20) interfaces in the same file. Do not modify any other interface.
  </action>
  <verify>
    <automated>rtk tsc</automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20240626000007_add_album_photos_is_day.sql exists and contains "ALTER TABLE album_photos ADD COLUMN IF NOT EXISTS is_day BOOLEAN DEFAULT NULL"
    - src/lib/db.types.ts AlbumPhoto interface contains "is_day: boolean | null" between alt and sort_order fields
    - rtk tsc exits 0 (no type errors introduced)
  </acceptance_criteria>
  <done>Migration file created, AlbumPhoto type updated, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Push migration to Supabase</name>
  <files>supabase/migrations/20240626000007_add_album_photos_is_day.sql</files>
  <read_first>
    - supabase/migrations/20240626000007_add_album_photos_is_day.sql (verify file exists before pushing)
  </read_first>
  <action>
    Run supabase db push to apply the new migration to the remote Supabase project. If supabase CLI is not available or auth fails, use the Supabase MCP apply_migration tool as fallback. The migration adds is_day boolean column to album_photos table with DEFAULT NULL. Verify the push succeeds without errors.
  </action>
  <verify>
    <automated>supabase db push 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push completes without error
    - album_photos table in remote Supabase project has is_day column
  </acceptance_criteria>
  <done>Migration applied to remote database, album_photos.is_day column exists</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB | Schema change applied to production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Migration SQL file | mitigate | IF NOT EXISTS makes migration idempotent; DEFAULT NULL prevents data corruption |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- supabase/migrations/ contains exactly 7 files (000001 through 000007)
- AlbumPhoto in db.types.ts has is_day field
- rtk tsc passes
</verification>

<success_criteria>
album_photos table has is_day column in remote DB, AlbumPhoto type includes is_day: boolean | null, TypeScript compiles
</success_criteria>

<output>
Create `.planning/phases/03-admin-controls/03-01-SUMMARY.md` when done
</output>
