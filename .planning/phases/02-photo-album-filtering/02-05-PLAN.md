---
phase: 02-photo-album-filtering
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql
autonomous: true
requirements:
  - CTR-08
gap_closure: true
gaps_closed:
  - "SC-5: Database indexes created on is_day columns for performance"

must_haves:
  truths:
    - "An index idx_portfolio_photos_is_day exists on portfolio_photos(is_day)"
    - "The albums(is_day) index (idx_albums_is_day) remains intact from migration 003"
    - "PortfolioGrid day/night queries no longer perform sequential scans on portfolio_photos"
  artifacts:
    - path: "supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql"
      provides: "Migration that adds missing index on portfolio_photos(is_day)"
      contains: "idx_portfolio_photos_is_day ON portfolio_photos(is_day)"
  key_links:
    - from: "supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql"
      to: "portfolio_photos.is_day"
      via: "CREATE INDEX IF NOT EXISTS"
      pattern: "idx_portfolio_photos_is_day"
---

<objective>
Close Gap 1 (BLOCKER — ROADMAP SC-5): Add the missing database index on `portfolio_photos(is_day)`.

Migration 003 created `idx_photos_is_day ON photos(is_day)` — but the application queries `portfolio_photos`, not `photos`. These are separate tables. Without an index on `portfolio_photos(is_day)`, every day/night filter query in `PortfolioGrid.tsx` performs a sequential scan.

Purpose: ROADMAP SC-5 requires "Database indexes created on is_day columns for performance." The albums index is correct. The portfolio_photos index is missing. This migration adds it.
Output: `supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql` — a standalone migration that adds the index without modifying any prior migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-photo-album-filtering/02-CONTEXT.md
@.planning/phases/02-photo-album-filtering/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 006 — add idx_portfolio_photos_is_day index</name>
  <files>supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql</files>
  <read_first>
    supabase/migrations/20240626000003_add_is_day_indexes.sql — read the existing (wrong) migration to understand what was created (idx_photos_is_day ON photos, idx_albums_is_day ON albums) and to confirm the albums index does NOT need to be recreated here.
  </read_first>
  <action>
    Create `supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql` with exactly the following content:

    - One CREATE INDEX statement: `CREATE INDEX IF NOT EXISTS idx_portfolio_photos_is_day ON portfolio_photos(is_day);`

    Do NOT touch migration 003. Do NOT recreate `idx_albums_is_day` (it already targets the correct table). Do NOT add any DROP, ALTER, or UPDATE statements. Do NOT wrap in a transaction block — single DDL statements do not require one. The IF NOT EXISTS guard makes this idempotent.

    The index name must be `idx_portfolio_photos_is_day` (not `idx_photos_is_day` which already exists on the wrong `photos` table).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql && grep -c "idx_portfolio_photos_is_day ON portfolio_photos" supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql</automated>
  </verify>
  <acceptance_criteria>
    1. File `supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql` exists.
    2. File contains exactly: `CREATE INDEX IF NOT EXISTS idx_portfolio_photos_is_day ON portfolio_photos(is_day);`
    3. File does NOT reference the `photos` table (wrong table from migration 003).
    4. File does NOT reference `idx_albums_is_day` (already correct in migration 003).
    5. `grep -c "portfolio_photos" supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql` returns 1.
    6. `grep -c "photos(is_day)" supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql` returns 0 (the old wrong name must not appear).
  </acceptance_criteria>
  <done>Migration file exists with the correct CREATE INDEX statement targeting portfolio_photos(is_day). The index name is idx_portfolio_photos_is_day. No references to the wrong `photos` table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration file → Supabase DB | DDL executed with service role; no user input, no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | supabase/migrations/20240626000006_*.sql | accept | File is version-controlled; static DDL with no interpolation |
| T-02-05-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
After task completes:
- `supabase/migrations/20240626000006_add_portfolio_photos_is_day_index.sql` exists
- File contains `CREATE INDEX IF NOT EXISTS idx_portfolio_photos_is_day ON portfolio_photos(is_day);`
- File does NOT contain references to table `photos` (without the `portfolio_` prefix)
- Migration count in supabase/migrations/ is 6 files
</verification>

<success_criteria>
ROADMAP SC-5 satisfied: database index on portfolio_photos(is_day) exists in a migration file, alongside the already-correct albums(is_day) index from migration 003. PortfolioGrid day/night filter queries can use the index instead of sequential scanning portfolio_photos.
</success_criteria>

<output>
Create `.planning/phases/02-photo-album-filtering/02-05-SUMMARY.md` when done
</output>
