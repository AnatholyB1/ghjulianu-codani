---
phase: 02-photo-album-filtering
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/albums/AlbumsDragTrack.tsx
  - src/app/albums/page.tsx
autonomous: true
requirements:
  - CTR-03
  - CTR-04
  - CTR-06
  - CTR-07

must_haves:
  truths:
    - "AlbumsDragTrack fetches its own albums from Supabase on mount and on every mode change"
    - "In day mode only albums with is_day = true OR is_day IS NULL are returned"
    - "In night mode only albums with is_day = false OR is_day IS NULL are returned"
    - "While a new fetch is in-flight the previous album list remains visible (no blank flash)"
    - "When the filtered result set is empty a text message 'No albums available in this mode yet' renders inside the track container"
    - "albums/page.tsx passes no data props to AlbumsDragTrack"
  artifacts:
    - path: "src/app/albums/AlbumsDragTrack.tsx"
      provides: "Self-fetching AlbumsDragTrack with day/night Supabase filter"
      contains: "useDayNight"
    - path: "src/app/albums/page.tsx"
      provides: "Thin shell that renders AlbumsDragTrack with no props"
      contains: "AlbumsDragTrack"
  key_links:
    - from: "src/app/albums/AlbumsDragTrack.tsx"
      to: "src/utils/supabase/client.ts"
      via: "createClient() call inside useEffect"
      pattern: "createClient"
    - from: "src/app/albums/AlbumsDragTrack.tsx"
      to: "src/hooks/useDayNight.ts"
      via: "useDayNight() destructuring mode"
      pattern: "useDayNight"
---

<objective>
Refactor AlbumsDragTrack to own its Supabase data fetch and apply day/night filtering via .or() on every mode change. Thin out albums/page.tsx to a shell with no server-side fetch.

Purpose: Implements CTR-03/04 (album filtering at DB level), CTR-06 (re-fetch on mode toggle), CTR-07 (empty state). The component already has the drag and category-filter logic — this plan adds the data ownership layer on top.
Output: Self-contained AlbumsDragTrack, thin albums/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-photo-album-filtering/02-CONTEXT.md
@.planning/phases/02-photo-album-filtering/02-01-SUMMARY.md
@src/app/albums/AlbumsDragTrack.tsx
@src/app/albums/page.tsx
@src/lib/db.types.ts
@src/hooks/useDayNight.ts
@src/utils/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor AlbumsDragTrack — internalize fetch with day/night filter</name>
  <files>src/app/albums/AlbumsDragTrack.tsx</files>
  <read_first>
    src/app/albums/AlbumsDragTrack.tsx — read full file; note all existing imports, the AlbumWithCat type alias, every useState and useRef declaration, the category-filter logic, and the drag event handlers
    src/hooks/useDayNight.ts — read to confirm the export name and return shape ({ mode })
    src/utils/supabase/client.ts — read to confirm the createClient export signature
    src/lib/db.types.ts — read Album and Category interfaces after the Task 2 edits from Plan 01
  </read_first>
  <action>
    Modify src/app/albums/AlbumsDragTrack.tsx:

    1. Remove the albums parameter from the component props. The component function signature becomes: export default function AlbumsDragTrack() with no props.

    2. Add the following imports at the top (after existing imports): import { createClient } from '@/utils/supabase/client' and import { useDayNight } from '@/hooks/useDayNight'.

    3. Inside the component, before any existing state declarations, add:
       - const { mode } = useDayNight()
       - useState<AlbumWithCat[]> initialized to an empty array — name the state variable albums and setter setAlbums. Keep the existing useState for the selected category filter as-is.

    4. Add a useEffect that lists mode in its dependency array. Inside the effect:
       a. Capture the current value of albums in a local variable (previousAlbums) so it can be preserved while loading — do NOT call setAlbums([]) at the start of the effect. This keeps the previous list visible during the in-flight request (per the loading state decision: show previous data while loading).
       b. Create the Supabase client: const supabase = createClient()
       c. Build the query: supabase.from('albums').select('*, category:categories(*)').order('sort_order', { ascending: false })
       d. Apply the day/night filter (per D-06):
          - If mode === 'day': chain .or('is_day.is.null,is_day.eq.true')
          - If mode === 'night': chain .or('is_day.is.null,is_day.eq.false')
       e. Await the result and call setAlbums(data ?? []).
       This useEffect replaces any top-level data fetching; the albums state variable now drives the existing category-filter logic that was previously operating on the prop.

    5. In the JSX render section, locate where albums (or the filtered list) is mapped. Add a conditional: if the filtered album list (after category filter applied) has length 0, render a div with the text "No albums available in this mode yet" inside the track container, instead of the empty track. The div should use className="text-center py-8 text-sm opacity-60" or a minimal inline style — discretion on exact className since the site uses Tailwind.

    6. Do NOT remove or alter: the AlbumWithCat type alias, the drag event handlers (onMouseDown, onMouseMove, onMouseUp, onTouchStart etc.), the category filter useState and its logic, the existing JSX structure for rendering album cards, any animation or transition classes.

    The Supabase filter syntax is exactly: .or('is_day.is.null,is_day.eq.true') for day and .or('is_day.is.null,is_day.eq.false') for night. Use these string literals verbatim.
  </action>
  <verify>
    <automated>grep -c "useDayNight" src/app/albums/AlbumsDragTrack.tsx && grep -c "createClient" src/app/albums/AlbumsDragTrack.tsx && grep -c "is_day.is.null" src/app/albums/AlbumsDragTrack.tsx</automated>
  </verify>
  <done>All three grep counts return at least 1. Component accepts no props. The file contains mode in a useEffect dependency array. rtk tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Thin albums/page.tsx — remove server-side fetch</name>
  <files>src/app/albums/page.tsx</files>
  <read_first>
    src/app/albums/page.tsx — read full file to identify: the createClient import, the async keyword on the default export, the supabase variable and .from('albums') query call, any other imports
  </read_first>
  <action>
    Rewrite src/app/albums/page.tsx to a thin shell:

    1. Remove the import of createClient from '@/utils/supabase/server'.
    2. Remove the async keyword from the default export function — it becomes a synchronous React Server Component (or can stay a Server Component, just not async).
    3. Remove the supabase variable declaration and the .from('albums').select(...) query.
    4. Remove the albums variable and any null-coalescing on it.
    5. Keep the import of AlbumsDragTrack.
    6. The return statement renders AlbumsDragTrack with no props: return <AlbumsDragTrack />.
    7. Keep any wrapping layout JSX (page container divs, title headings, or section elements) that existed around the AlbumsDragTrack render — only remove the data-fetching code.

    If albums/page.tsx had any metadata export (export const metadata = ...) keep it untouched.
  </action>
  <verify>
    <automated>grep -c "createClient" src/app/albums/page.tsx; echo "exit: $?"</automated>
  </verify>
  <done>grep for createClient returns 0. The file renders AlbumsDragTrack with no props. rtk tsc --noEmit exits 0. rtk next build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase | Supabase client call from browser; filtered by Supabase RLS + .or() filter |
| Zustand store → component | mode value from useDayNight(); trust boundary is within-process, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | AlbumsDragTrack Supabase query | accept | Supabase RLS on albums table controls visibility; is_public column enforced at DB level |
| T-02-03 | Tampering | mode value from Zustand | accept | mode is 'day' | 'night' union type; only two branches possible in .or() filter construction |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages |
</threat_model>

<verification>
After both tasks complete:
- rtk tsc --noEmit exits 0
- rtk next build exits 0
- albums/page.tsx contains no createClient import and no async keyword
- AlbumsDragTrack.tsx contains useDayNight, createClient, and .or('is_day.is.null,...') patterns
- Toggling day/night mode on /albums triggers a new Supabase fetch and updates the displayed albums
</verification>

<success_criteria>
Albums page fetches filtered albums per day/night mode. Mode change triggers re-fetch. Empty state message renders when filtered result is empty. No server-side data fetch in albums/page.tsx.
</success_criteria>

<output>
Create .planning/phases/02-photo-album-filtering/02-02-SUMMARY.md when done
</output>
