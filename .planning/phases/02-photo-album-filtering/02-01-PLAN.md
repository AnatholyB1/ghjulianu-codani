---
phase: 02-photo-album-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20240626000005_reset_is_day_to_null.sql
  - src/lib/db.types.ts
autonomous: true
requirements:
  - CTR-05
  - CTR-08

must_haves:
  truths:
    - "All existing portfolio_photos rows have is_day = NULL after migration runs"
    - "All existing albums rows have is_day = NULL after migration runs"
    - "PortfolioPhoto interface has is_day field typed as boolean | null"
    - "Album interface has is_day field typed as boolean | null"
    - "AlbumPhoto interface does NOT have an is_day field"
  artifacts:
    - path: "supabase/migrations/20240626000005_reset_is_day_to_null.sql"
      provides: "SQL migration that sets is_day = NULL on all portfolio_photos and albums rows"
      contains: "UPDATE portfolio_photos"
    - path: "src/lib/db.types.ts"
      provides: "Updated TypeScript interfaces with is_day: boolean | null"
      contains: "is_day: boolean | null"
---

<objective>
Lay the foundational data layer for Phase 2 filtering: add migration 005 that resets all existing records to is_day = NULL (untagged / shows in both modes), and update the TypeScript interfaces so downstream components can reference the field with proper types.

Purpose: Plans 02, 03, and 04 all depend on the DB schema state and type definitions being accurate before they refactor fetching logic. Running this first ensures no type errors in Wave 2 plans.
Output: Migration SQL file, updated db.types.ts with is_day on Album and PortfolioPhoto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-photo-album-filtering/02-CONTEXT.md
@src/lib/db.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 005 — reset is_day to NULL</name>
  <files>supabase/migrations/20240626000005_reset_is_day_to_null.sql</files>
  <read_first>
    supabase/migrations/20240626000004_backfill_is_day.sql — read to confirm existing migration pattern (transaction block, table names used)
    src/lib/db.types.ts — read to confirm table/column names match TypeScript interfaces
  </read_first>
  <action>
    Create supabase/migrations/20240626000005_reset_is_day_to_null.sql.

    The file must contain a single transaction block. Inside the transaction:
    1. Run UPDATE portfolio_photos SET is_day = NULL — no WHERE clause (targets all rows).
    2. Run UPDATE albums SET is_day = NULL — no WHERE clause (targets all rows).

    The column is_day already exists on both tables from migrations 001 and 002. This migration does NOT ALTER the column definition — it only resets row values to NULL so Phase 3 tagging starts from a clean slate (per D-05).

    Do NOT add any DROP or ALTER statements. Do NOT touch album_photos (per D-09).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20240626000005_reset_is_day_to_null.sql && grep -c "UPDATE portfolio_photos" supabase/migrations/20240626000005_reset_is_day_to_null.sql</automated>
  </verify>
  <done>File exists and contains UPDATE statements for both portfolio_photos and albums. No ALTER or DROP statements present.</done>
</task>

<task type="auto">
  <name>Task 2: Update db.types.ts — add is_day to Album and PortfolioPhoto interfaces</name>
  <files>src/lib/db.types.ts</files>
  <read_first>
    src/lib/db.types.ts — read the full file to locate Album, PortfolioPhoto, and AlbumPhoto interface definitions before editing
  </read_first>
  <action>
    Edit src/lib/db.types.ts to add is_day: boolean | null to two interfaces (per D-08):

    In the PortfolioPhoto interface: add is_day: boolean | null after the alt field.

    In the Album interface: add is_day: boolean | null after the background_url field.

    Do NOT add is_day to AlbumPhoto (per D-09). Do not modify any other interfaces or types in the file. Do not reformat unrelated lines.
  </action>
  <verify>
    <automated>grep -c "is_day: boolean | null" src/lib/db.types.ts</automated>
  </verify>
  <done>grep returns 2 (one match in PortfolioPhoto, one in Album). AlbumPhoto interface contains no is_day field. TypeScript compilation produces no new errors: rtk tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → DB | SQL executed with Supabase service role; no user input in migration file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | supabase/migrations/*.sql | accept | Migration files are version-controlled; no dynamic content injected |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
After both tasks complete:
- rtk tsc --noEmit exits 0 with no new type errors
- supabase/migrations/20240626000005_reset_is_day_to_null.sql exists and contains UPDATE statements for both tables
- src/lib/db.types.ts contains is_day: boolean | null on both Album and PortfolioPhoto, absent on AlbumPhoto
</verification>

<success_criteria>
Migration file ready for apply. TypeScript interfaces updated. Wave 2 plans can proceed with correct type definitions available.
</success_criteria>

<output>
Create .planning/phases/02-photo-album-filtering/02-01-SUMMARY.md when done
</output>
