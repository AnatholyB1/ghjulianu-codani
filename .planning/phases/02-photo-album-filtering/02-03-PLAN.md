---
phase: 02-photo-album-filtering
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/portfolio/PortfolioGrid.tsx
  - src/app/portfolio/page.tsx
autonomous: true
requirements:
  - CTR-03
  - CTR-04
  - CTR-06
  - CTR-07

must_haves:
  truths:
    - "PortfolioGrid fetches its own photos from Supabase on mount and on every mode change"
    - "In day mode only photos with is_day = true OR is_day IS NULL are returned"
    - "In night mode only photos with is_day = false OR is_day IS NULL are returned"
    - "Fisher-Yates shuffle runs client-side after each fetch completes"
    - "While a new fetch is in-flight the previous photo list remains visible"
    - "When the filtered result set is empty a text message 'No photos available in this mode yet' renders"
    - "portfolio/page.tsx passes no data props to PortfolioGrid"
  artifacts:
    - path: "src/app/portfolio/PortfolioGrid.tsx"
      provides: "Self-fetching PortfolioGrid with day/night Supabase filter and client-side shuffle"
      contains: "useDayNight"
    - path: "src/app/portfolio/page.tsx"
      provides: "Thin shell that renders PortfolioGrid with no props"
      contains: "PortfolioGrid"
  key_links:
    - from: "src/app/portfolio/PortfolioGrid.tsx"
      to: "src/utils/supabase/client.ts"
      via: "createClient() call inside useEffect"
      pattern: "createClient"
    - from: "src/app/portfolio/PortfolioGrid.tsx"
      to: "src/hooks/useDayNight.ts"
      via: "useDayNight() destructuring mode"
      pattern: "useDayNight"
---

<objective>
Refactor PortfolioGrid to own its Supabase data fetch, apply day/night filtering via .or() on every mode change, and run the Fisher-Yates shuffle client-side after each fetch. Thin out portfolio/page.tsx to a shell with no server-side fetch.

Purpose: Implements CTR-03/04 (photo filtering at DB level), CTR-06 (re-fetch on mode toggle), CTR-07 (empty state). The shuffle currently lives in the Server Component; this plan migrates it into the Client Component fetch callback.
Output: Self-contained PortfolioGrid, thin portfolio/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-photo-album-filtering/02-CONTEXT.md
@.planning/phases/02-photo-album-filtering/02-01-SUMMARY.md
@src/app/portfolio/PortfolioGrid.tsx
@src/app/portfolio/page.tsx
@src/lib/db.types.ts
@src/hooks/useDayNight.ts
@src/utils/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor PortfolioGrid — internalize fetch, shuffle, and day/night filter</name>
  <files>src/app/portfolio/PortfolioGrid.tsx</files>
  <read_first>
    src/app/portfolio/PortfolioGrid.tsx — read full file; note existing imports, the photos prop type, every useState/useRef declaration, the lightbox state, the IntersectionObserver setup, and the JSX render structure
    src/app/portfolio/page.tsx — read to extract the Fisher-Yates shuffle implementation verbatim so it can be moved into PortfolioGrid unchanged
    src/hooks/useDayNight.ts — confirm export name and return shape
    src/utils/supabase/client.ts — confirm createClient export
    src/lib/db.types.ts — confirm PortfolioPhoto interface shape after Plan 01 edits
  </read_first>
  <action>
    Modify src/app/portfolio/PortfolioGrid.tsx:

    1. Remove the photos parameter from the component props. The function signature becomes: export default function PortfolioGrid() with no props.

    2. Add imports: import { createClient } from '@/utils/supabase/client' and import { useDayNight } from '@/hooks/useDayNight'. Both go at the top with existing imports.

    3. Inside the component, add before existing state:
       - const { mode } = useDayNight()
       - useState<PortfolioPhoto[]> initialized to an empty array — name the state variable photos and setter setPhotos. The existing useState for lightbox (useState<number | null>) remains unchanged.

    4. Copy the Fisher-Yates shuffle function body from src/app/portfolio/page.tsx into PortfolioGrid.tsx as a local helper function defined inside the component or as a module-level function at the top of the file — either placement is acceptable. The shuffle mutates a copy of the array (slice() first) and returns the shuffled result. Preserve the exact algorithm from page.tsx without changes.

    5. Add a useEffect with mode in its dependency array:
       a. Do NOT call setPhotos([]) at the start — keep showing the previous list while the new request is in-flight (per loading state decision: show previous data, no spinner).
       b. const supabase = createClient()
       c. Build the query: supabase.from('portfolio_photos').select('*')
       d. Apply day/night filter (per D-06):
          - If mode === 'day': chain .or('is_day.is.null,is_day.eq.true')
          - If mode === 'night': chain .or('is_day.is.null,is_day.eq.false')
       e. Await the result. Apply the Fisher-Yates shuffle to data before calling setPhotos. Call setPhotos(shuffle(data ?? [])).

    6. In the JSX render section, add an empty-state conditional: if photos.length === 0 after the fetch has run, render a div with text "No photos available in this mode yet". Use className="text-center py-8 text-sm opacity-60" (or equivalent minimal Tailwind). This div replaces the photo grid when empty — do not render both.

    7. Do NOT remove or alter: the lightbox useState, the IntersectionObserver ref/effect, the Lightbox component render, the existing photo card JSX structure, any animation or intersection-based className logic.

    The Supabase filter strings are exactly: .or('is_day.is.null,is_day.eq.true') for day and .or('is_day.is.null,is_day.eq.false') for night — use these verbatim.
  </action>
  <verify>
    <automated>grep -c "useDayNight" src/app/portfolio/PortfolioGrid.tsx && grep -c "createClient" src/app/portfolio/PortfolioGrid.tsx && grep -c "is_day.is.null" src/app/portfolio/PortfolioGrid.tsx</automated>
  </verify>
  <done>All three grep counts return at least 1. Component accepts no props. useEffect dependency array contains mode. rtk tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Thin portfolio/page.tsx — remove server-side fetch and shuffle</name>
  <files>src/app/portfolio/page.tsx</files>
  <read_first>
    src/app/portfolio/page.tsx — read full file to identify: the createClient import from server, the async keyword, the supabase query, the Fisher-Yates shuffle code, the photos variable, any header/section JSX around the PortfolioGrid render, and any metadata export
  </read_first>
  <action>
    Rewrite src/app/portfolio/page.tsx:

    1. Remove the import of createClient from '@/utils/supabase/server'.
    2. Remove the async keyword from the default export function.
    3. Remove the supabase variable, the .from('portfolio_photos') query, and the Fisher-Yates shuffle code.
    4. Remove the photos variable and its null-coalescing.
    5. Keep the import of PortfolioGrid.
    6. Change the PortfolioGrid render to pass no props: <PortfolioGrid />.
    7. Keep any wrapping layout JSX (page title, section container, header elements) that existed around the PortfolioGrid render — only remove data-fetching and shuffle code.
    8. If a metadata export (export const metadata = ...) exists, keep it unchanged.

    After editing, portfolio/page.tsx should be a minimal synchronous Server Component that renders the page shell and delegates all data concerns to PortfolioGrid.
  </action>
  <verify>
    <automated>grep -c "createClient" src/app/portfolio/page.tsx; echo "exit: $?"</automated>
  </verify>
  <done>grep for createClient returns 0. The file renders PortfolioGrid with no props. No shuffle code present in page.tsx. rtk tsc --noEmit exits 0. rtk next build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase | Supabase client call from browser; filtered by RLS + .or() filter |
| Zustand store → component | mode value from useDayNight(); in-process, not external user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | PortfolioGrid Supabase query | accept | portfolio_photos is public-read content; Supabase RLS governs access |
| T-02-05 | Tampering | mode value from Zustand | accept | mode is 'day' | 'night' union; only two filter branches exist |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages |
</threat_model>

<verification>
After both tasks complete:
- rtk tsc --noEmit exits 0
- rtk next build exits 0
- portfolio/page.tsx contains no createClient import, no async, no shuffle
- PortfolioGrid.tsx contains useDayNight, createClient, and .or('is_day.is.null,...') patterns
- Fisher-Yates shuffle exists inside PortfolioGrid.tsx
- Toggling day/night mode on /portfolio triggers a new Supabase fetch and re-shuffles the displayed photos
</verification>

<success_criteria>
Portfolio page fetches filtered photos per day/night mode. Mode change triggers re-fetch and re-shuffle. Empty state message renders when filtered result is empty. No server-side data fetch in portfolio/page.tsx.
</success_criteria>

<output>
Create .planning/phases/02-photo-album-filtering/02-03-SUMMARY.md when done
</output>
