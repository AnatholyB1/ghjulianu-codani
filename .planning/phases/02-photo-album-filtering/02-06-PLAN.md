---
phase: 02-photo-album-filtering
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/dayNightStore.ts
autonomous: true
requirements:
  - CTR-03
gap_closure: true
gaps_closed:
  - "SC-4: Default state shows all content when no preference set (or defaults to day)"

must_haves:
  truths:
    - "A first-time visitor with no localStorage sees the site in day mode"
    - "The Zustand store initializes mode to 'day', not 'night'"
    - "Returning visitors with a persisted preference still get their saved mode (persist middleware unchanged)"
  artifacts:
    - path: "src/store/dayNightStore.ts"
      provides: "Zustand store with initial mode set to 'day'"
      contains: "mode: 'day'"
  key_links:
    - from: "src/store/dayNightStore.ts"
      to: "useDayNight hook consumers"
      via: "initial state"
      pattern: "mode: 'day'"
---

<objective>
Close Gap 2 (BLOCKER — ROADMAP SC-4): Fix the initial default mode from `'night'` to `'day'` in `src/store/dayNightStore.ts`.

ROADMAP SC-4 states "Default state shows all content when no preference set (or defaults to day)." The store currently initializes with `mode: 'night'` (line 14), so a first-time visitor with no localStorage entry sees night mode — not day mode as required.

The fix is a one-character-word change: `'night'` → `'day'` in the initial state object. The persist middleware, BroadcastChannel wiring, toggleMode logic, and all other store behavior remain unchanged.

Purpose: SC-4 compliance. The site must default to day mode for users who have never set a preference.
Output: Updated `src/store/dayNightStore.ts` with `mode: 'day'` as the initial value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-photo-album-filtering/02-CONTEXT.md
@.planning/phases/02-photo-album-filtering/02-VERIFICATION.md
@src/store/dayNightStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change initial mode from 'night' to 'day' in dayNightStore.ts</name>
  <files>src/store/dayNightStore.ts</files>
  <read_first>
    src/store/dayNightStore.ts — read the full file before editing. Locate the `create&lt;DayNightState&gt;()` call and the object literal inside `persist(...)`. The initial state object has `mode: 'night'` — this is the only line to change.
  </read_first>
  <action>
    Edit `src/store/dayNightStore.ts`. Change the initial state value on the line that reads `mode: 'night'` to `mode: 'day'`.

    Exact target line (current): `      mode: 'night',`
    Exact replacement: `      mode: 'day',`

    Do NOT change anything else in the file:
    - The `persist` middleware configuration (`name: 'day-night-storage'`, `storage: createJSONStorage(...)`) must remain exactly as-is — returning users with a stored preference in localStorage will still get their saved mode.
    - The `setMode`, `toggleMode` implementations must remain unchanged.
    - The `dayNightBroadcastChannel.subscribe(...)` block at the bottom must remain unchanged.
    - All imports must remain unchanged.

    This is a single-line edit. The storage key `'day-night-storage'` is unaffected — returning users who previously toggled to night will still load night from their stored preference. Only first-time visitors (no localStorage entry) will now default to day instead of night.
  </action>
  <verify>
    <automated>grep -n "mode: 'day'" src/store/dayNightStore.ts</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -c "mode: 'day'" src/store/dayNightStore.ts` returns 1 (exactly one occurrence — the initial state value).
    2. `grep -c "mode: 'night'" src/store/dayNightStore.ts` returns 0 (the old default must be gone).
    3. `grep -c "persist" src/store/dayNightStore.ts` returns at least 2 (persist import and persist call still present — middleware not removed).
    4. `grep -c "day-night-storage" src/store/dayNightStore.ts` returns 1 (storage key unchanged).
    5. `grep -c "dayNightBroadcastChannel" src/store/dayNightStore.ts` returns at least 2 (broadcast wiring untouched).
    6. `rtk tsc --noEmit` exits 0 — no TypeScript errors introduced.
  </acceptance_criteria>
  <done>src/store/dayNightStore.ts initializes mode to 'day'. No other store behavior is altered. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → Zustand store | User-controlled storage read on hydration; persist middleware handles deserialization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | localStorage 'day-night-storage' | accept | Low-value target; mode value is validated as 'day' | 'night' union type by TypeScript; invalid values fall back to store default |
| T-02-06-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no packages |
</threat_model>

<verification>
After task completes:
- `grep -c "mode: 'day'" src/store/dayNightStore.ts` returns 1
- `grep -c "mode: 'night'" src/store/dayNightStore.ts` returns 0
- `rtk tsc --noEmit` exits 0
- All other store contents (persist config, toggleMode, broadcast subscribe) unchanged
</verification>

<success_criteria>
ROADMAP SC-4 satisfied: the store defaults to day mode. A user with no localStorage preference sees day mode on first visit. Returning users retain their persisted preference via the unchanged Zustand persist middleware.
</success_criteria>

<output>
Create `.planning/phases/02-photo-album-filtering/02-06-SUMMARY.md` when done
</output>
