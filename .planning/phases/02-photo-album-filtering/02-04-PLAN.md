---
phase: 02-photo-album-filtering
plan: 04
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/page.tsx
autonomous: true
requirements:
  - CTR-03
  - CTR-04
  - CTR-06

must_haves:
  truths:
    - "Home page recent albums collage re-fetches from Supabase whenever day/night mode changes"
    - "In day mode the recentAlbums query filters is_day = true OR is_day IS NULL"
    - "In night mode the recentAlbums query filters is_day = false OR is_day IS NULL"
    - "The recentAlbums useEffect dependency array includes mode"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page with day/night filtered recentAlbums query"
      contains: "useDayNight"
  key_links:
    - from: "src/app/page.tsx"
      to: "supabase.from('albums')"
      via: "recentAlbums useEffect"
      pattern: "is_day.is.null"
---

<objective>
Add day/night filtering to the recentAlbums fetch on the home page (per D-03). The component already uses createClient() and a useEffect — this plan adds useDayNight(), derives the filter condition from mode, appends the .or() call to the existing query, and adds mode to the useEffect dependency array.

Purpose: Implements D-03 (home page album collage filtered by day/night). The home page is already a Client Component with client-side Supabase; this is a targeted addition to the existing fetch logic.
Output: Updated src/app/page.tsx where recentAlbums re-fetches on mode change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-photo-album-filtering/02-CONTEXT.md
@.planning/phases/02-photo-album-filtering/02-01-SUMMARY.md
@src/app/page.tsx
@src/hooks/useDayNight.ts
@src/lib/db.types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add day/night filter to recentAlbums fetch in page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>
    src/app/page.tsx — read full file; identify: all existing imports at the top, the useDayNight import if already present, the recentAlbums useState declaration, the recentAlbums useEffect (the one calling .from('albums')), the exact query chain (.select, .eq, .order, .limit), and the current dependency array (currently empty [])
    src/hooks/useDayNight.ts — confirm import path and return shape ({ mode })
  </read_first>
  <action>
    Edit src/app/page.tsx:

    1. If useDayNight is not already imported, add: import { useDayNight } from '@/hooks/useDayNight' with the other imports.

    2. Inside the component function body, add const { mode } = useDayNight() near the top of the component, grouped with other hook calls. If useDayNight is already called for the toggle button or navbar, do NOT add a duplicate call — reuse the existing destructured mode variable.

    3. Locate the recentAlbums useEffect. It currently chains: .from('albums').select('id,title,slug,cover_url,created_at,year').eq('is_public', true).order('created_at', { ascending: false }).limit(3)

    After the .eq('is_public', true) call and before .order(...), insert the day/night filter (per D-06):
       - If mode === 'day': chain .or('is_day.is.null,is_day.eq.true')
       - If mode === 'night': chain .or('is_day.is.null,is_day.eq.false')

    Implement this as a conditional chain: build the base query up to .eq('is_public', true), assign to a variable, then conditionally append the .or() call based on mode, then chain .order().limit(). Alternatively, use a ternary inline in the chain — either pattern is acceptable as long as the correct .or() string is applied for the current mode on every execution.

    4. Change the useEffect dependency array from [] to [mode]. This ensures the effect re-runs when mode changes (per D-02 and CTR-06).

    5. Do NOT change: the .select() columns list, the .eq('is_public', true) filter, the .order() or .limit() calls, the setRecentAlbums call and its data casting, any other useEffect in the file, the recentAlbums useState type, the JSX that renders recentAlbums.

    The filter strings are exactly: .or('is_day.is.null,is_day.eq.true') for day and .or('is_day.is.null,is_day.eq.false') for night — use these verbatim.
  </action>
  <verify>
    <automated>grep -c "useDayNight" src/app/page.tsx && grep -c "is_day.is.null" src/app/page.tsx && grep -c "\[mode\]" src/app/page.tsx</automated>
  </verify>
  <done>All three grep counts return at least 1. The recentAlbums useEffect dependency array contains mode. rtk tsc --noEmit exits 0. rtk next build exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Supabase | recentAlbums fetch from browser; filtered by is_public=true + is_day .or() + RLS |
| Zustand store → component | mode value from useDayNight(); in-process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | page.tsx recentAlbums query | accept | is_public=true filter already enforced; albums not in public set are excluded regardless of is_day |
| T-02-07 | Tampering | mode value from Zustand | accept | mode is 'day' | 'night' union; only two filter branches, no user-controlled string interpolation |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no new packages |
</threat_model>

<verification>
After the task completes:
- rtk tsc --noEmit exits 0
- rtk next build exits 0
- src/app/page.tsx contains useDayNight, .or('is_day.is.null,...'), and [mode] in the recentAlbums useEffect deps
- Toggling day/night mode on the home page triggers a new Supabase fetch for recentAlbums and updates the collage
</verification>

<success_criteria>
Home page album collage fetches filtered albums per day/night mode. Mode change triggers re-fetch. No regressions to other home page sections or existing album display logic.
</success_criteria>

<output>
Create .planning/phases/02-photo-album-filtering/02-04-SUMMARY.md when done
</output>
